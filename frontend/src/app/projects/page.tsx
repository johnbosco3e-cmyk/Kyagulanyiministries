import Image from 'next/image'; import Link from 'next/link';
import { PageHero } from '@/components/ui/page-hero'; import { projects } from '@/lib/content';
export const metadata={title:'Projects'};
export default function ProjectsPage(){return <main><PageHero eyebrow="Projects" title="Transparent action. Measurable change." text="Explore community-led projects, track their progress, and see exactly where your support makes a difference." image="https://images.unsplash.com/photo-1488521787991-ed7bbaae773c?auto=format&fit=crop&w=2000&q=85"/><section className="py-20"><div className="container-site"><div className="mb-10 flex flex-wrap gap-3">{['All projects','Education','Clean Water','Livelihoods','Active','Completed'].map((x,i)=><button key={x} className={`rounded-full px-5 py-2.5 text-xs font-bold ${i===0?'bg-[#071b33] text-white':'border border-slate-200'}`}>{x}</button>)}</div><div className="grid gap-8 lg:grid-cols-3">{projects.map(p=><Link href={`/projects/${p.slug}`} key={p.slug} className="group overflow-hidden rounded-3xl border border-slate-200"><div className="relative aspect-[16/11]"><Image src={p.image} alt="" fill className="object-cover transition duration-700 group-hover:scale-105"/></div><div className="p-7"><p className="eyebrow">{p.category} · {p.location}</p><h2 className="mt-3 font-serif text-3xl font-semibold text-[#071b33]">{p.title}</h2><p className="mt-3 text-sm leading-6 text-[#657184]">{p.summary}</p><div className="mt-6 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-[#d5a84b]" style={{width:`${p.progress}%`}}/></div><p className="mt-2 text-xs font-bold">{p.progress}% funded</p></div></Link>)}</div></div></section></main>}
